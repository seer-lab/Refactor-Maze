<stuff>

<code>
float getCircleCircumference (float radius)
{
    return <smell id="0">3.14</smell> * radius * <smell>2</smell>;
}

float getCircleArea(float radius)
{
    return <smell id="0">3.14</smell> * pow(radius, <smell>2</smell>);
}

float getSphereVolume(float radius)
{
    return (4 * <smell id="0">3.14</smell> * pow(radius, 3))/3;
}

float getSphereSurfaceArea(float radius)
{
    return 4 * <smell id="0">3.14</smell> * pow(radius, <smell>2</smell>);
} 
</code>
<correctcode>
float getCircleCircumference (float radius)
{
    return pi * radius * 2;
}

float getCircleArea(float radius)
{
    return pi * pow(radius, 2);
}

float getSphereVolume(float radius)
{
    return (4 * pi * pow(radius, 3))/3;
}

float getSphereSurfaceArea(float radius)
{
    return 4 * pi * pow(radius, 2);
} </correctcode>
</stuff>